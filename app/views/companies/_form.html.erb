<%= simple_form_for @company do |c| %>
Select Industry: 
<%= select_tag(:category_id, options_for_select(@categories), {:selected => :category_id}) %>
<%= c.file_field :company_img %>
  <%= c.input :name,label: "Company Name" %>
<%= c.input :description %>
<%= c.input :location, label: "Website" %>
<%= c.button :submit, :class => "btn btn-custom2" %>
<% end %>